main(){

  // int number1; //but here the value is not assigned so it will give error   
  // print(number1);

  int ? number1;//here value is not assigned so it will take a null value in dafault
  print(number1);

  number1 =  50;//this is nonnullable variable cause it is assigned
  print(number1); 
}
